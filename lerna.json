{
    "version": "independent",
    "npmClient": "yarn",
    "useWorkspaces": true,
    "packages": ["packages/*", "docs/*"],
    "command": {
        "publish": {
            "ignoreChanges": ["**/docs/**", "**/*.md", "**/tests/*", "**/*.map"]
        }
    }
}
